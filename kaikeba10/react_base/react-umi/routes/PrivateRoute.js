import { Redirect } from 'umi'

export default props => {
  // if (Math.random() > 0.5) {
  //   return <Redirect to='/login'></Redirect>
  // }
  // return (
  //   <div>
  //     <h1>PrivateRoute (routes/PrivateRoute.js)</h1>
  //     {props.children}
  //   </div>
  // )
}